# Vercel serverless requirements — ONLY pure-Python packages or packages
# with confirmed Python 3.14 (cp314) pre-built wheels.
# Packages removed vs backend/requirements.txt:
#   pillow    — not used by the API at all
#   rapidfuzz — fuzzy_match.py falls back to stdlib difflib automatically
#   alembic   — migrations run locally, not inside the function
#   uvicorn[standard] → uvicorn (no uvloop/httptools needed for serverless)
fastapi==0.111.0
uvicorn==0.29.0
sqlalchemy==2.0.29
pg8000==1.31.2
python-dotenv==1.0.1
openai==1.30.1
anthropic==0.28.0
python-multipart==0.0.9
openpyxl==3.1.2
python-dateutil==2.9.0
cryptography==42.0.7
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
pydantic-settings==2.3.0
